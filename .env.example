# Database
DB_USER=
DB_PASSWORD=
DB_SERVER=
DB_NAME=

# Mailer (SMTP)
# Use SMTP settings for production where possible (SendGrid/SES/Mailgun or company mail relay)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_REJECT_UNAUTHORIZED=true

# Gmail auth (legacy / dev): use an App Password when 2FA is enabled
GMAIL_USER=
GMAIL_PASS=

# SendGrid API key (preferred on PaaS where SMTP ports may be blocked)
SENDGRID_API_KEY=
SENDGRID_FROM=your-sender@your-domain.com

# Optional
MAIL_FROM=

# App
FRONTEND_URL=
PORT=3000

# Test helper
TEST_MAIL_TO=
